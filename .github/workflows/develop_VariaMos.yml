# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - VariaMos

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

#    - uses: azure/docker-login@v1
#      name: Login to ACR
#      with:
#        login-server: 'variamos.azurecr.io'
#        username: ${{secrets.ACRUSERNAME}}
#        password: ${{secrets.ACRUSERPASSWORD}}

    - name: Login to ACR
      uses: docker/login-action@v1
      with:
        registry: variamos.azurecr.io
        username: ${{secrets.ACRUSERNAME}}
        password: ${{secrets.ACRUSERPASSWORD}}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: variamos.azurecr.io/secrets.ACRUSERNAME/variamos:${{ github.sha }}
        file: ./Dockerfile

#  deploy:
#    runs-on: ubuntu-latest
#    needs: build
#    environment:
#      name: 'development'
#      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

#    steps:
#    - name: Deploy to Azure Web App
#      id: deploy-to-webapp
#      uses: azure/webapps-deploy@v2
#      with:
#        app-name: 'VariaMos'
#        slot-name: 'dev'
#        publish-profile: ${{ secrets.AzureAppService_PublishProfile_7303893fa7264f6cb628bcfc51d974c8 }}
#        images: 'index.docker.io/${{ secrets.AzureAppService_ContainerUsername_dcb921d499e6434c901f5f15b252ad3b }}/variamos:${{ github.sha }}'